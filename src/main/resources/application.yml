server:
  port: 8091

spring:
  application:
    name: AuthorizationServer

# DataSource Configuration
  datasource:
    #url: jdbc:postgresql://auth-server.cpsiaygcukgh.af-south-1.rds.amazonaws.com:5432/postgres

    url: jdbc:postgresql://localhost:5432/auth_server

    username: postgres
    password: Lodav72#
    driver-class-name: org.postgresql.Driver

# JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: profdamuleli@gmail.com
    password: atvy oedw wzyd gxlp
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.debug: true