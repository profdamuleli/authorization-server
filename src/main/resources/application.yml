server:
  port: 8091

spring:
  application:
    name: AuthorizationServer

# DataSource Configuration
  datasource:
    url: jdbc:postgresql://postgres-1.cpsiaygcukgh.af-south-1.rds.amazonaws.com:5432/postgres

    #url: jdbc:postgresql://localhost:5432/authserverdb

    username: postgres
    password: Lodav72#
    driver-class-name: org.postgresql.Driver

# JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
